{
  "private": true,
  "workspaces": [
    "api",
    "react-client"
  ],
  "scripts": {
    "start:api": "yarn workspace api start",
    "start:dev:api": "yarn workspace api start:dev",
    "start:debug:api": "yarn workspace api start:debug"
  }
}